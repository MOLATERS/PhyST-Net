\section{Method}
\label{sec:methodology}
PhyST-Net is an ExG-aware spatio-temporal graph Transformer designed for network-structured node forecasting with known external drivers and domain constraints. Given the inputs defined in Section~\ref{sec:problem_definition}, the model follows the pipeline in Fig.~\ref{fig:physt_architecture}. Historical target states and historical ExG are first embedded into node-level temporal representations. AGSP converts the temporal history into continuous tokens. A Transformer backbone learns temporal dependencies. R-STMF serves as a graph adapter that maps temporal node states to graph-enhanced node states. K-AMC injects future ExG into the latent representation. PI-DCH finally decodes the conditioned states into feasible forecasts respecting domain-specific bounds.

\input{sections/architecture_figure}

\subsection{Overall Framework}
\label{sec:overall_framework}
For a mini-batch, let $\mathbf{X}\in\mathbb{R}^{B\times T\times N\times D_x}$ denote historical target states, $\mathbf{E}^h\in\mathbb{R}^{B\times T\times D_h}$ denote historical ExG, and $\mathbf{E}^f\in\mathbb{R}^{B\times H\times D_f}$ denote future ExG. The model produces $\hat{\mathbf{Y}}\in\mathbb{R}^{B\times H\times N\times D_y}$. The computation can be written as
\begin{equation}
Z = \Phi_{patch}(\mathbf{X},\mathbf{E}^{h},M_G),
\label{eq:overall_patch}
\end{equation}
\begin{equation}
H_T = \Phi_{temp}(Z),\quad H_G = \Phi_{graph}(H_T,A,M_G),
\label{eq:overall_temp_graph}
\end{equation}
\begin{equation}
H_F = \Phi_{exg}(H_G,\mathbf{E}^{f}),\quad \hat{\mathbf{Y}} = \Phi_{head}(H_F,\mathbf{E}^{f},\Omega),
\label{eq:overall_exg_head}
\end{equation}
where $\Phi_{patch}$ is AGSP, $\Phi_{temp}$ is the temporal Transformer encoder, $\Phi_{graph}$ is the graph adapter (R-STMF), $\Phi_{exg}$ is future-ExG conditioning (K-AMC), and $\Phi_{head}$ is the physics-informed prediction head (PI-DCH).

\subsection{Adaptive Gaussian-soft-windowed Patching}
\label{sec:agsp}
Transformer-based forecasters conventionally mitigate sequence length by aggregating observations into discrete temporal patches. While computationally convenient, fixed non-overlapping patches inevitably induce boundary artifacts when the underlying physical signal transverses a patch boundary. AGSP supersedes rigid patch assignments with learnable Gaussian soft-windows. This differentiable formulation empowers the architecture to autonomously allocate dense receptive fields to highly transient events and broader, smoother fields to stable operational regimes.

\begin{figure}[!t]
\centering
\begin{tikzpicture}[
    font=\sffamily,
    >=Stealth,
    x=1.3cm, 
    y=1.3cm,
    axis/.style={->, line width=0.8pt, draw=black!70},
    signal/.style={gray!75, line width=0.9pt},
    hard/.style={line width=1pt},
    soft/.style={line width=1pt, fill opacity=0.25},
    warn/.style={->, line width=0.8pt, draw=red!80!black},
    note/.style={font=\tiny, align=center}
]
    % (a) Rigid
    \begin{scope}[shift={(0, 2.2)}]
        \node[anchor=west, font=\bfseries\small] at (0, 1.2) {Traditional Rigid Hard Patches};
        \draw[axis] (0,0) -- (3.2,0) node[right] {$t$};
        \draw[signal] plot[domain=0.2:3.0, samples=60] (\x,{0.6 + 0.3*sin(deg(2*\x)) + 0.1*sin(deg(12*\x))});
        \draw[blue!70!black, hard] (0.4, 0) rectangle (1.2, 1.0);
        \draw[orange!85!black, hard] (1.2, 0) rectangle (2.0, 1.0);
        \draw[green!60!black, hard] (2.0, 0) rectangle (2.8, 1.0);
        \node[note, text=red!80!black, font=\bfseries] (art) at (1.6, 1.4) {Boundary Artifacts};
        \draw[warn] (art.west) -- (1.2, 1.05);
        \draw[warn] (art.east) -- (2.0, 1.05);
    \end{scope}
    
    % (b) Soft
    \begin{scope}[shift={(0, 0)}]
        \node[anchor=west, font=\bfseries\small] at (0, 1.2) {Proposed Adaptive Gaussian Soft-windows};
        \draw[axis] (0,0) -- (3.2,0) node[right] {$t$};
        \draw[signal] plot[domain=0.2:3.0, samples=60] (\x,{0.6 + 0.3*sin(deg(2*\x)) + 0.1*sin(deg(12*\x))});
        \path[fill=blue!20, draw=blue, soft] (0.2,0) plot[domain=0.2:1.8, samples=40] (\x,{1.1*exp(-((\x-1)^2)/0.15)}) -- (1.8,0) -- cycle;
        \path[fill=orange!20, draw=orange, soft] (0.8,0) plot[domain=0.8:2.4, samples=40] (\x,{0.9*exp(-((\x-1.6)^2)/0.1)}) -- (2.4,0) -- cycle;
        \path[fill=green!20, draw=green!60!black, soft] (1.4,0) plot[domain=1.4:3.0, samples=40] (\x,{1.2*exp(-((\x-2.2)^2)/0.2)}) -- (3.0,0) -- cycle;
        \node[note, anchor=west] at (0.2, -0.3) {* Overlapping regions ensure temporal continuity.};
    \end{scope}
\end{tikzpicture}
\caption{Mechanics of AGSP. By replacing discrete sequence chunking with continuous, differentiable density windows, the model autonomously mitigates boundary artifacts and atmospheric noise.}
\label{fig:agsp_detail}
\end{figure}

For node $n$ and temporal window $i$, the unnormalized weight at time index $\tau\in\{1,\ldots,T\}$ is
\begin{equation}
W_i(\tau)=\exp\left(-\frac{(\tau-\mu_i)^2}{2\sigma_i^2+\epsilon}\right),
\label{eq:agsp_window}
\end{equation}
where $\mu_i$ and $\sigma_i$ are learnable center and bandwidth parameters. The centers $\mu_i$ are initialized to uniform grid locations across the historical window to encourage an ordered temporal partition. The weights are normalized across $P$ windows:
\begin{equation}
\tilde{W}_i(\tau)=\frac{W_i(\tau)}{\sum_{j=1}^{P}W_j(\tau)+\epsilon}.
\label{eq:agsp_norm}
\end{equation}
Let $\bar{x}_{n}(\tau)$ denote the embedded historical state. The token for node $n$ and patch $i$ is
\begin{equation}
z_{n,i}=\psi\left(\sum_{\tau=1}^{T}\tilde{W}_i(\tau)\bar{x}_{n}(\tau)\right)+e_n+p_i,
\label{eq:agsp_token}
\end{equation}
where $\psi(\cdot)$ is a linear projection, $e_n$ is a node embedding, and $p_i$ is a temporal patch embedding.

\subsection{Temporal Representation Learning}
\label{sec:temporal_encoder}
Adopting the global token philosophy \cite{wang2024timexer}, the token tensor $Z$ is concatenated with a learnable global token $z_{global} \in \mathbb{R}^{B \times N \times 1 \times d}$ for each node. This global token serves as a sequence-level memory bottleneck. In PhyST-Net, rather than a naive standard Transformer, STEnc incorporates a targeted cross-attention mechanism where the global token uniquely cross-attends to the embedded historical exogenous features:
\begin{equation}
z_{global}' = z_{global} + \operatorname{CrossAttention}(z_{global}, \mathbf{E}^h, \mathbf{E}^h).
\label{eq:cross_attention}
\end{equation}
This contextually enriched sequence is then processed through the feed-forward networks, yielding the final temporal representation $H_T=\operatorname{STEnc}([Z \parallel z_{global}])$. Crucially, PhyST-Net leverages the global token to separate the node's state into two distinct manifolds: a local-variation patch state $U \triangleq H_{T,1:P}$ and a macro-trend global state $R \triangleq H_{T,P+1}$.

\subsection{Relational Spatio-Temporal Manifold Fusion (R-STMF)}
\label{sec:rstmf}
R-STMF serves as the graph adapter. Unlike conventional graph operators, R-STMF performs a **Latent Patch Manifold Decomposition** on the temporal representation $H_T$. For each node $n$, we apply a 1D depth-wise convolution along the patch dimension to extract the seasonal component $H_{S}$, while the residual represents the trend component $H_{R}$:
\begin{equation}
H_{S} = \operatorname{GELU}(\operatorname{Conv1d}(H_T)), \quad H_{R} = H_T - H_{S}.
\label{eq:rstmf_decomp}
\end{equation}

\begin{figure}[!t]
\centering
\begin{tikzpicture}[
    font=\sffamily,
    >=Stealth,
    x=1.2cm, 
    y=1.2cm,
    module/.style={draw, rounded corners=3pt, very thick, align=center, minimum height=0.5cm},
    cond/.style={module, fill=violet!8, draw=violet!60!black, minimum width=1.8cm},
    arrow/.style={->, line width=0.8pt, draw=black!75},
    note/.style={font=\tiny, align=center}
]
    % HT node
    \node[module, minimum width=4cm] (ht) at (1.5, 4.2) {$H_T$ (Latent Representation)};
    
    % Decomp block
    \draw[arrow] (ht.south) -- ++(0,-0.7) node[midway, right, note] {Latent Patch Decomp.};
    \node[cond] (hs) at (0, 2.7) {Seasonal $H_S$};
    \node[cond] (hr) at (3.0, 2.7) {Trend $H_R$};
    
    % Graph operations
    \node[circle, draw, thick, fill=teal!5] (gs) at (0, 1.4) {GC};
    \node[circle, draw, thick, fill=teal!5] (gr) at (3.0, 1.4) {GC};
    \draw[draw=red!70!black, dashed, line width=1.2pt, rounded corners] (-1.0, 1.0) rectangle (4.0, 1.8);
    \node[note, text=red!80!black, font=\bfseries] at (1.5, 0.7) {Distance-Masked Top-$K$ Interaction};
    
    \draw[arrow] (hs.south) -- (gs.north);
    \draw[arrow] (hr.south) -- (gr.north);
    
    % Fusion
    \node[circle, draw, very thick, fill=white] (fuse) at (1.5, 0) {$\oplus$};
    \draw[arrow] (gs.south) |- (fuse.west);
    \draw[arrow] (gr.south) |- (fuse.east);
    \draw[arrow] (fuse.south) -- ++(0,-0.6) node[below, font=\bfseries] {$H_G$ (Graph-Enhanced State)};
    \node[note, right] at (1.7, 0) {Gated Fusion};
\end{tikzpicture}
\caption{Detailed architecture of R-STMF. Latent patch sequences are decomposed into seasonal and trend manifolds, which independently undergo distance-masked graph convolution prior to gated fusion.}
\label{fig:rstmf_detail}
\end{figure}

\subsubsection{Distance-Masked Relational Pruning}
To enforce spatial locality, we introduce a **Distance-Masked Top-$K$ Pruning** strategy. Given a binary mask $\mathcal{M}_{dist}$ based on a feasible interaction radius, the attention scores are:
\begin{equation}
S_{c,ij} = \begin{cases} \frac{Q_c(K_c)^{\top}}{\sqrt{d_m}} & \text{if } \mathcal{M}_{ij}=1 \\ -\infty & \text{otherwise} \end{cases}, \quad A_c=\operatorname{Softmax}\left(\operatorname{TopK}(S_c, K_c)\right).
\label{eq:rstmf_routing}
\end{equation}

\subsection{Future-ExG Conditioning (K-AMC)}
\label{sec:kamc}
K-AMC serves as a generalized interface for known systemic drivers, injecting future exogenous drivers $\mathbf{E}^f$ into the latent representations. An alignment operator $\Gamma$ maps the future horizon into variate tokens. These are passed through a Kolmogorov-Arnold Network (KAN) block which parameterizes transformations using learnable univariate B-spline functions $\phi(x) = w_b \operatorname{SiLU}(x) + \sum_{i=1}^{k} c_i B_i(x)$.

\begin{figure}[!t]
\centering
\begin{tikzpicture}[
    font=\sffamily,
    >=Stealth,
    x=1.3cm, 
    y=1.3cm,
    module/.style={draw, rounded corners=3pt, very thick, align=center},
    input/.style={module, fill=blue!6, draw=blue!60!black, minimum width=2cm, minimum height=0.6cm},
    kan/.style={module, fill=teal!7, draw=teal!60!black, minimum width=2.8cm, minimum height=1.6cm},
    cond/.style={module, fill=violet!8, draw=violet!60!black, minimum width=1.0cm, minimum height=0.5cm},
    arrow/.style={->, line width=0.8pt, draw=black!75},
    note/.style={font=\tiny, align=center}
]
    % Left side
    \node[input] (ef) at (0, 3.8) {Future ExG $\mathbf{E}^f$};
    \draw[arrow] (ef.south) -- ++(0,-0.5) node[midway, right, note] {Temporal Align};
    
    \node[kan] (kanbox) at (0, 2.2) {};
    \node[anchor=north, font=\bfseries\small] at (0, 3.0) {KAN Block};
    \fill (-0.7, 2.5) circle (1.5pt); \fill (0.7, 2.5) circle (1.5pt); \fill (0, 1.7) circle (1.5pt);
    \draw[violet, ->, line width=1.1pt] (-0.7, 2.5) .. controls (-0.6, 2.1) and (-0.2, 1.9) .. (0, 1.7) node[midway, left, note] {$\phi(x)$};
    \draw[violet, ->, line width=1.1pt] (0.7, 2.5) .. controls (0.6, 2.1) and (0.2, 1.9) .. (0, 1.7);

    \node[cond] (gamma) at (-0.7, 0.6) {$\gamma$};
    \node[cond] (beta) at (0.7, 0.6) {$\beta$};
    \draw[line width=1pt, draw=black!75] (0, 1.4) -- (0, 1.1);
    \draw[arrow] (0, 1.1) -| (gamma.north);
    \draw[arrow] (0, 1.1) -| (beta.north);

    % Right side
    \node[module, fill=white, minimum width=1.8cm, minimum height=0.6cm] (hg) at (3.5, 3.8) {$H_G$};
    \node[draw, circle, thick, fill=white, minimum size=6mm, inner sep=0pt] (mul) at (3.5, 2.7) {$\otimes$};
    \node[draw, circle, thick, fill=white, minimum size=6mm, inner sep=0pt] (add1) at (3.5, 1.6) {$\oplus$};
    \node[draw, circle, thick, fill=white, minimum size=6mm, inner sep=0pt] (add2) at (3.5, 0.5) {$\oplus$};
    \node[module, fill=blue!6, draw=blue!60!black, minimum width=2.6cm, minimum height=0.7cm] (hf) at (3.5, -0.7) {\textbf{Conditioned State} $H_F$};

    \draw[arrow] (hg.south) -- (mul.north);
    \draw[arrow] (mul.south) -- (add1.north);
    \draw[arrow] (add1.south) -- (add2.north);
    \draw[arrow] (add2.south) -- (hf.north);

    \draw[arrow] (gamma.east) -- ++(2.0, 0) |- (mul.west);
    \draw[arrow] (beta.east) -- ++(0.6, 0) |- (add1.west);
    \draw[arrow] (hg.east) -- ++(0.8, 0) |- (add2.east);
    \node[note, anchor=west] at (4.4, 2.2) {Res. Scale\\$\times 0.1$};
\end{tikzpicture}
\caption{Mechanics of K-AMC. Future exogenous drivers are aligned and processed through spline-based KAN edges to produce dynamic modulation parameters.}
\label{fig:kamc_detail}
\end{figure}

The KAN block produces modulation parameters $[\gamma,\beta]=\operatorname{KAN}(\bar{\mathbf{E}}^f)$. The conditioned latent state is then obtained via element-wise scale and shift: $H_F=H_G+\lambda\left(\gamma\odot H_G+\beta-H_G\right)$.

\subsection{Physics-Informed Dynamic Capping Head (PI-DCH)}
\label{sec:pidch}
PI-DCH instantiates the constraint operator $\mathcal{C}_{\Omega}$ through a dual-stage enforcement strategy. While $\hat{Y}_{soft} = \tilde{Y}+u\odot(C_{dyn}-\tilde{Y})$ provides the differentiable path necessary for optimization, we define the final deployment-ready forecast $\hat{\mathbf{Y}}$ through a Euclidean projection $\hat{\mathbf{Y}} = \operatorname{Proj}_{\mathcal{K}}(\hat{Y}_{soft}) \triangleq \max(0, \min(\hat{Y}_{soft}, C_{dyn}))$, where $\mathcal{K} = [0, C_{dyn}]$.

\subsection{Computational Complexity and Optimization}
PhyST-Net reduces the complexity of dense ST-Transformers from $\mathcal{O}(N T^2 + T N^2)$ to $\mathcal{O}(N P^2 + P K N)$ through its decoupled topology. The model parameters $\theta$ are optimized by minimizing a multi-objective loss $\mathcal{L} = \mathcal{L}_{mae} + \lambda_{\Omega}(\mathcal{L}_{neg} + \mathcal{L}_{cap})$.

\begin{algorithm}[!t]
\caption{PhyST-Net Forward Pass}
\label{alg:physt_net}
\begin{algorithmic}[1]
\REQUIRE Input tuple $\mathcal{X}_t = (\mathbf{X}, \mathbf{E}^h, \mathbf{E}^f, A, M_G)$, Domain knowledge $\Omega$.
\ENSURE Feasible network forecast $\hat{\mathbf{Y}}$.
\STATE Compress $\mathbf{X}, \mathbf{E}^h$ via AGSP into tokens $Z$.
\STATE Process $Z$ with cross-attention on $\mathbf{E}^h$ to get $H_T$.
\STATE Decompose $H_T$ and propagate over masked graph (R-STMF) $\to H_G$.
\STATE Modulate $H_G$ with future $\mathbf{E}^f$ via KAN (K-AMC) $\to H_F$.
\STATE Shape and project output via barrier gate (PI-DCH) $\to \hat{\mathbf{Y}}$.
\RETURN $\hat{\mathbf{Y}}$.
\end{algorithmic}
\end{algorithm}
